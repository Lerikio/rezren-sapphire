= form_for @adherent do |f|
  - if @adherent.errors.any?
    #error_explanation
      %h2= "#{pluralize(@adherent.errors.count, "error")} prohibited this adherent from being saved:"
      %ul
        - @adherent.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
