%h1 Listing vlans

%table
  %tr
    %th Number
    %th Name
    %th
    %th
    %th

  - @vlans.each do |vlan|
    %tr
      %td= vlan.number
      %td= vlan.name
      %td= link_to 'Show', vlan
      %td= link_to 'Edit', edit_vlan_path(vlan)
      %td= link_to 'Destroy', vlan, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Vlan', new_vlan_path
