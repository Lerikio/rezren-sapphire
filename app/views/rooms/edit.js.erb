$("#modal-window").html("<%= escape_javascript(render 'edit') %>");

// Tooltips
$("[rel='tooltip']").tooltip();

// Selects
$('#buildings-select').select2({
  placeholder: "Bâtiment"
});

$("form").on("ajax:success", function(event, data, status, xhr){
    $('#modal-window').modal('hide');
    reload_rooms();
  });